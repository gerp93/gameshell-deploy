# Per-game deployment config for card-judge.
# Sourced by the gameshell-deploy scripts. See gameshell-deploy/examples/deploy.conf.

APP_NAME=card-judge
ENV_PREFIX=CARD_JUDGE
DB_NAME=CARD_JUDGE
HTTP_PORT=2016
GIT_REPO=gerp93/card-judge
GIT_UPSTREAM=GrantFBarnes/card-judge
